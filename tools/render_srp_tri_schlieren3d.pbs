#!/bin/bash
#PBS -N srp_sch3d
#PBS -q cx
#PBS -l walltime=03:00:00
#PBS -l select=1:ncpus=4:mem=64gb
#PBS -j oe
#PBS -o /rds/general/ephemeral/user/jq319/ephemeral/aws_offload_ibm/srp_tri_L2/post_latest_plt09000/schlieren3d/pbs.log

set -euo pipefail

source /etc/profile.d/modules.sh
source /etc/profile.d/z00_lmod.sh
module purge
module load tools/prod yt/4.3.0-foss-2022a VTK/9.2.2-foss-2022a

CASE=/rds/general/ephemeral/user/jq319/ephemeral/aws_offload_ibm/srp_tri_L2
OUT=${CASE}/post_latest_plt09000/schlieren3d
CACHE=${OUT}/plt09000_density_gradient_volume.npz
export MPLCONFIGDIR=${TMPDIR:-/tmp}/mpl-${PBS_JOBID}
export OMP_NUM_THREADS=4
mkdir -p "${MPLCONFIGDIR}" "${OUT}"

date -u
hostname
xvfb-run -a -s "-screen 0 3200x2000x24" \
  python3 "${CASE}/render_srp_tri_schlieren3d.py" \
    --plotfile "${CASE}/plot/plt09000" \
    --stl "${CASE}/trinozzle.stl" \
    --output-dir "${OUT}" \
    --cache "${CACHE}" \
    --image-size 3200 2000 \
    2>&1 | tee "${OUT}/run.log"
date -u
